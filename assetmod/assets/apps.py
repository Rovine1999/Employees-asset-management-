from django.apps import AppConfig

# This is the configuration file for the assets app. It is used to register the app with the Django project.
class AssetsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'assets'
